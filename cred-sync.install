post_install() {
    mkdir -p /etc/cred-sync
    if command -v cred-sync-onboard &>/dev/null; then
        sudo -u "$SUDO_USER" cred-sync-onboard || true
    fi
}

post_upgrade() {
    :
}

pre_remove() {
    :
} 