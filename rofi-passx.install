post_install() {
    mkdir -p /etc/rofi-passx
    if command -v rofi-passx-setup &>/dev/null; then
        sudo -u "$SUDO_USER" rofi-passx-setup || true
    fi
}

post_upgrade() {
    :
}

pre_remove() {
    :
} 